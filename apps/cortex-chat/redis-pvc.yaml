apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data
  namespace: cortex-chat
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
