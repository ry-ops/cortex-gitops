apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-ingress
  namespace: cortex-chat
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  ingress:
  # Allow from Traefik ingress controller
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: kube-system
      podSelector:
        matchLabels:
          app.kubernetes.io/name: traefik
  # Allow from within cortex-chat namespace (pod-to-pod)
  - from:
    - podSelector: {}
  # Allow from cortex-system (for orchestrator, monitoring, etc.)
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: cortex-system
