apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-egress
  namespace: cortex-security
spec:
  podSelector: {}
  policyTypes:
  - Egress
  egress:
  # Allow DNS
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: kube-system
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
  # Allow internal cluster communication
  - to:
    - podSelector: {}
  # Allow to cortex-system (for postgres)
  - to:
    - namespaceSelector:
        matchLabels:
          name: cortex-system
    ports:
    - protocol: TCP
      port: 5432
  # Allow to all namespaces (for security scanning)
  - to:
    - namespaceSelector: {}
  # Allow external HTTPS/HTTP (for CVE databases, etc)
  - ports:
    - protocol: TCP
      port: 443
    - protocol: TCP
      port: 80
