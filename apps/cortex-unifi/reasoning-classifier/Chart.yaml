apiVersion: v2
name: reasoning-classifier
description: |
  Reasoning Classifier Layer - Lightweight intent classification.
  Uses Qwen2-0.5B for fast query routing when keywords don't match.
  KEDA-managed, scales 0→1 on demand.
type: application
version: 0.1.0
appVersion: "1.0.0"
