apiVersion: v2
name: execution-unifi-api
description: |
  UniFi API Execution Layer - Handles all UniFi API operations.
  Site Manager (cloud) + Network Application (local) APIs.
  KEDA-managed, scales 0→1 on demand.
type: application
version: 0.1.0
appVersion: "1.0.0"
