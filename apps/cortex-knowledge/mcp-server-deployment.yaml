apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  namespace: cortex-knowledge
  labels:
    app: mcp-server
    app.kubernetes.io/part-of: cortex
    cortex.ai/layer: "true"
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: mcp-server
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-server
  namespace: cortex-knowledge
  labels:
    app: mcp-server
    app.kubernetes.io/part-of: cortex
    cortex.ai/layer: "true"
  annotations:
    cortex.ai/layer-domain: "knowledge-tools"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-server
  template:
    metadata:
      labels:
        app: mcp-server
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3000"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: mcp-knowledge-tools
      containers:
      - name: server
        image: node:18-alpine
        imagePullPolicy: IfNotPresent
        workingDir: /app
        command: ["node", "/app/server.js"]
        ports:
        - containerPort: 3000
          protocol: TCP
        env:
        - name: PORT
          value: "3000"
        - name: LAYER_NAME
          value: "cortex-knowledge"
        - name: LAYER_DOMAIN
          value: "knowledge-tools"
        - name: KNOWLEDGE_GRAPH_API
          value: "http://knowledge-graph-api.cortex-knowledge:8080"
        - name: ELASTICSEARCH_HOST
          value: "knowledge-elasticsearch.cortex-knowledge:9200"
        - name: MONGODB_HOST
          value: "knowledge-mongodb.cortex-knowledge:27017"
        - name: PHOENIX_HOST
          value: "phoenix.cortex-knowledge:4000"
        - name: LOG_LEVEL
          value: "INFO"
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
        volumeMounts:
        - name: server-code
          mountPath: /app
      volumes:
      - name: server-code
        configMap:
          name: knowledge-mcp-server-code
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mcp-knowledge-tools
  namespace: cortex-knowledge
  labels:
    cortex.ai/layer: "true"
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: mcp-knowledge-tools
  namespace: cortex-knowledge
  labels:
    cortex.ai/layer: "true"
rules:
- apiGroups: [""]
  resources: ["pods", "services", "configmaps"]
  verbs: ["get", "list", "watch"]
- apiGroups: ["apps"]
  resources: ["deployments", "statefulsets"]
  verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: mcp-knowledge-tools
  namespace: cortex-knowledge
  labels:
    cortex.ai/layer: "true"
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: mcp-knowledge-tools
subjects:
- kind: ServiceAccount
  name: mcp-knowledge-tools
  namespace: cortex-knowledge
