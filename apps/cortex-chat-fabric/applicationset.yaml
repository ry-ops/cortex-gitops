apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cortex-chat-fabric
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - name: chat-activator
            path: apps/cortex-chat-fabric/chat-activator
  template:
    metadata:
      name: "chat-fabric-{{name}}"
      namespace: argocd
      labels:
        app.kubernetes.io/part-of: cortex-chat-fabric
        cortex.cortex-io.dev/fabric: chat
    spec:
      project: default
      source:
        repoURL: https://github.com/cortex-io/cortex-gitops.git
        targetRevision: HEAD
        path: "{{path}}"
        helm:
          releaseName: "{{name}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: cortex-chat
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - PruneLast=true
