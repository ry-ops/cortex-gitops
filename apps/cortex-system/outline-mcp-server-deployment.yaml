apiVersion: apps/v1
kind: Deployment
metadata:
  name: outline-mcp-server
  namespace: cortex-system
  labels:
    app: outline-mcp-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: outline-mcp-server
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: outline-mcp-server
    spec:
      terminationGracePeriodSeconds: 30
      initContainers:
      - name: setup-source
        image: busybox
        command:
        - sh
        - -c
        - |
          mkdir -p /repo/mcp_outline
          cp /source-server/server.py /repo/mcp_outline/
          cp /source-init/__init__.py /repo/mcp_outline/
          echo "Source files copied"
          ls -laR /repo
        volumeMounts:
        - mountPath: /repo
          name: repo
        - mountPath: /source-server
          name: source-server
        - mountPath: /source-init
          name: source-init
      containers:
      - name: app
        image: python:3.11-slim
        command:
        - /bin/bash
        - -c
        - |
          set -e
          echo "Installing dependencies..."
          pip install --no-cache-dir mcp>=1.9.4 httpx>=0.27.0 pydantic>=2.0.0

          echo "Copying HTTP wrapper..."
          cp /wrapper/mcp-http-wrapper.py /repo/wrapper.py

          echo "Starting Outline MCP HTTP Server on port 3004..."
          python /repo/wrapper.py
        env:
        - name: OUTLINE_API_TOKEN
          valueFrom:
            secretKeyRef:
              name: outline-mcp-server-secret
              key: OUTLINE_API_TOKEN
        - name: OUTLINE_BASE_URL
          valueFrom:
            secretKeyRef:
              name: outline-mcp-server-secret
              key: OUTLINE_BASE_URL
        - name: OUTLINE_TIMEOUT
          valueFrom:
            secretKeyRef:
              name: outline-mcp-server-secret
              key: OUTLINE_TIMEOUT
        - name: MCP_COMMAND
          value: "python -m mcp_outline.server"
        - name: PORT
          value: "3004"
        ports:
        - containerPort: 3004
          name: http
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 2
        livenessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        lifecycle:
          preStop:
            exec:
              command:
              - /bin/sh
              - -c
              - sleep 15
        volumeMounts:
        - mountPath: /repo
          name: repo
        - mountPath: /wrapper
          name: http-wrapper
        workingDir: /repo
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 400m
            memory: 256Mi
      volumes:
      - name: repo
        emptyDir: {}
      - name: http-wrapper
        configMap:
          name: mcp-http-wrapper
      - name: source-server
        configMap:
          name: outline-mcp-server-py
      - name: source-init
        configMap:
          name: outline-mcp-init-py
