apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cortex-security-fabric
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - name: security-activator
            path: apps/cortex-security-fabric/security-activator
  template:
    metadata:
      name: "security-fabric-{{name}}"
      namespace: argocd
      labels:
        app.kubernetes.io/part-of: cortex-security-fabric
        cortex.cortex-io.dev/fabric: security
    spec:
      project: default
      source:
        repoURL: https://github.com/cortex-io/cortex-gitops.git
        targetRevision: HEAD
        path: "{{path}}"
        helm:
          releaseName: "{{name}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: cortex-system
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - PruneLast=true
